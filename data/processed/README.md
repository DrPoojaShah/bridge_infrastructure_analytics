# Processed Data

The processed dataset used for machine learning and Power BI is **not included** in this repository because of GitHub file-size limitations.

The processed dataset can be regenerated by running:

```bash
python src/prepare_powerbi_data.py
```

Output:

```text
data/processed/bridge_powerbi_data.csv
```

The script performs the same preprocessing steps described in the project notebook.